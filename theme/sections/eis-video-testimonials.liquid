{%- comment -%}
  EIS — Video testimonials
  ------------------------------------------------------------------
  A row of 9:16 UGC clips. Nothing heavier than a poster image loads until
  someone taps play — hosted videos use preload="none", YouTube/Vimeo use a
  click-to-load facade so no third-party player script runs on page load.

  A block with no video yet renders the design's striped placeholder, so the
  layout is right before the real clips arrive.
{%- endcomment -%}

{% render 'eis-fonts' %}

{% style %}
  {% render 'eis-tokens', id: section.id, s: section.settings %}

  .eis-ugc {
    background: {{ section.settings.color_background }};
    font-family: var(--eis-sans);
  }
  .eis-ugc__inner {
    max-width: var(--eis-max);
    margin: 0 auto;
    padding: {{ section.settings.padding_top }}px 16px {{ section.settings.padding_bottom }}px;
  }
  .eis-ugc__title {
    font-family: var(--eis-serif);
    font-size: 27px;
    font-weight: 600;
    color: var(--eis-maroon);
    text-align: center;
    margin: 0 0 18px;
    line-height: 1.2;
  }
  .eis-ugc__grid {
    display: flex;
    gap: 9px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 2px;
    /* the native bar is hidden because the section draws its own below */
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .eis-ugc__grid::-webkit-scrollbar { display: none; }
  .eis-ugc__item {
    flex: 0 0 calc((100% - ({{ section.settings.cards_in_view }} - 1) * 9px) / {{ section.settings.cards_in_view }});
    min-width: 0;
    scroll-snap-align: start;
  }
  .eis-ugc__frame {
    position: relative;
    aspect-ratio: {{ section.settings.card_ratio }};
    border-radius: 11px;
    overflow: hidden;
    background: repeating-linear-gradient(135deg, #EDE1CE 0 8px, #F9F1E4 8px 16px);
    width: 100%;
    padding: 0;
    border: 0;
    display: block;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .eis-ugc__frame img,
  .eis-ugc__frame video,
  .eis-ugc__frame iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    border: 0;
    display: block;
  }
  .eis-ugc__hint {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 8px;
    font: 9px ui-monospace, monospace;
    color: var(--eis-soft);
    text-align: center;
    padding: 0 6px;
  }
  .eis-ugc__play {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 36px;
    height: 36px;
    border-radius: 999px;
    background: rgba(123, 19, 21, .9);
    color: #FFF7E6;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    z-index: 2;
    pointer-events: none;
  }
  .eis-ugc__frame.is-playing .eis-ugc__play,
  .eis-ugc__frame.is-playing .eis-ugc__hint { display: none; }
  .eis-ugc__by { margin-top: 9px; text-align: center; }
  .eis-ugc__name-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
  }
  .eis-ugc__tick { color: var(--eis-green); font-size: 11px; line-height: 1; flex: none; }
  .eis-ugc__name { font-size: 11.5px; font-weight: 700; color: var(--eis-ink); }
  .eis-ugc__city { font-size: 10px; color: var(--eis-muted); line-height: 1.4; margin-top: 1px; }

  /* Scroll bar. Reflects position and is draggable; hidden by JS when
     everything already fits, so it never appears as dead furniture. */
  .eis-ugc__bar {
    position: relative;
    height: 4px;
    max-width: 180px;
    margin: 16px auto 0;
    border-radius: 99px;
    background: #EDE1CE;
    cursor: pointer;
    touch-action: none;
  }
  .eis-ugc__bar-thumb {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    border-radius: 99px;
    background: var(--eis-maroon);
    transition: left .08s linear, width .08s linear;
  }
  @media (prefers-reduced-motion: reduce) {
    .eis-ugc__bar-thumb { transition: none; }
  }
{% endstyle %}

<div class="eis-ugc">
  <div class="eis-ugc__inner">
    {%- if section.settings.heading != blank -%}
      <h2 class="eis-ugc__title">{{ section.settings.heading | newline_to_br }}</h2>
    {%- endif -%}

    <div class="eis-ugc__grid" data-eis-scroller>
      {%- for block in section.blocks -%}
        {%- liquid
          assign has_hosted = false
          assign has_embed = false
          if block.settings.video != blank
            assign has_hosted = true
          elsif block.settings.video_url != blank
            assign has_embed = true
          endif
        -%}
        <div class="eis-ugc__item" {{ block.shopify_attributes }}>
          {%- if has_hosted -%}
            <div class="eis-ugc__frame is-playing">
              {{
                block.settings.video
                | video_tag:
                  controls: true,
                  preload: 'none',
                  playsinline: true,
                  image_size: '480x'
              }}
            </div>
          {%- elsif has_embed -%}
            <button
              type="button"
              class="eis-ugc__frame"
              data-eis-embed="{{ block.settings.video_url.id }}"
              data-eis-type="{{ block.settings.video_url.type }}"
              aria-label="Play video: {{ block.settings.name | escape }}"
            >
              {%- if block.settings.poster != blank -%}
                {{ block.settings.poster | image_url: width: 540 | image_tag: widths: '180, 270, 360, 540', sizes: '33vw', loading: 'lazy', alt: '' }}
              {%- endif -%}
              <span class="eis-ugc__play" aria-hidden="true">▶</span>
            </button>
          {%- else -%}
            <div class="eis-ugc__frame">
              {%- if block.settings.poster != blank -%}
                {{ block.settings.poster | image_url: width: 540 | image_tag: widths: '180, 270, 360, 540', sizes: '33vw', loading: 'lazy', alt: '' }}
              {%- else -%}
                <span class="eis-ugc__hint">{{ block.settings.placeholder_note }}</span>
              {%- endif -%}
              <span class="eis-ugc__play" aria-hidden="true">▶</span>
            </div>
          {%- endif -%}

          {%- if block.settings.name != blank -%}
            <div class="eis-ugc__by">
              <div class="eis-ugc__name-row">
                <span class="eis-ugc__tick" aria-hidden="true">&#10004;</span>
                <span class="eis-ugc__name">{{ block.settings.name }}</span>
              </div>
              {%- if block.settings.city != blank -%}
                <div class="eis-ugc__city">{{ block.settings.city }}</div>
              {%- endif -%}
            </div>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>

    <div
      class="eis-ugc__bar"
      data-eis-bar
      role="scrollbar"
      aria-orientation="horizontal"
      aria-label="Scroll testimonials"
      hidden
    >
      <div class="eis-ugc__bar-thumb" data-eis-thumb></div>
    </div>
  </div>
</div>

<script>
  (function () {
    var root = document.getElementById('shopify-section-{{ section.id }}');
    if (!root) return;
    var buttons = root.querySelectorAll('[data-eis-embed]');

    for (var i = 0; i < buttons.length; i++) {
      buttons[i].addEventListener('click', function () {
        if (this.classList.contains('is-playing')) return;
        var id = this.getAttribute('data-eis-embed');
        var type = this.getAttribute('data-eis-type');
        var src =
          type === 'vimeo'
            ? 'https://player.vimeo.com/video/' + id + '?autoplay=1'
            : 'https://www.youtube.com/embed/' + id + '?autoplay=1&playsinline=1';

        var frame = document.createElement('iframe');
        frame.setAttribute('src', src);
        frame.setAttribute('allow', 'autoplay; encrypted-media; picture-in-picture');
        frame.setAttribute('allowfullscreen', '');
        frame.setAttribute('title', this.getAttribute('aria-label') || 'Video');
        this.appendChild(frame);
        this.classList.add('is-playing');
      });
    }

    /* ---- scroll bar ---- */
    var scroller = root.querySelector('[data-eis-scroller]');
    var bar = root.querySelector('[data-eis-bar]');
    var thumb = root.querySelector('[data-eis-thumb]');
    if (!scroller || !bar || !thumb) return;

    var MIN_THUMB = 18; /* percent — keeps the thumb grabbable on long rows */

    function sync() {
      var max = scroller.scrollWidth - scroller.clientWidth;
      if (max <= 1) { bar.hidden = true; return; }
      bar.hidden = false;
      var width = Math.max(MIN_THUMB, (scroller.clientWidth / scroller.scrollWidth) * 100);
      thumb.style.width = width + '%';
      thumb.style.left = (scroller.scrollLeft / max) * (100 - width) + '%';
      bar.setAttribute('aria-valuenow', Math.round((scroller.scrollLeft / max) * 100));
    }

    function seek(clientX) {
      var r = bar.getBoundingClientRect();
      var p = Math.min(1, Math.max(0, (clientX - r.left) / r.width));
      scroller.scrollLeft = p * (scroller.scrollWidth - scroller.clientWidth);
    }

    var dragging = false;
    bar.addEventListener('pointerdown', function (e) {
      dragging = true;
      bar.setPointerCapture(e.pointerId);
      seek(e.clientX);
    });
    bar.addEventListener('pointermove', function (e) { if (dragging) seek(e.clientX); });
    bar.addEventListener('pointerup', function () { dragging = false; });
    bar.addEventListener('pointercancel', function () { dragging = false; });

    scroller.addEventListener('scroll', sync, { passive: true });
    window.addEventListener('resize', sync);
    sync();
  })();
</script>

{% schema %}
{
  "name": "EIS — Video testimonials",
  "tag": "section",
  "class": "eis-section eis-section--ugc",
  "max_blocks": 9,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "info": "Line breaks are kept.",
      "default": "Real People, Real Relief"
    },
    {
      "type": "range",
      "id": "cards_in_view",
      "label": "Clips in view",
      "min": 1.5,
      "max": 3.5,
      "step": 0.1,
      "default": 2.2,
      "info": "Shopify allows one decimal place here. 2.2 shows two whole clips plus a sliver of the next, which is what tells people the row scrolls."
    },
    {
      "type": "select",
      "id": "card_ratio",
      "label": "Clip shape",
      "options": [
        { "value": "9 / 16", "label": "9:16 — matches phone video" },
        { "value": "5 / 9", "label": "5:9 — taller" },
        { "value": "1 / 2", "label": "1:2 — tallest" }
      ],
      "default": "9 / 16",
      "info": "Taller than 9:16 crops the sides of the video."
    },
    {
      "type": "header",
      "content": "Colours"
    },
    {
      "type": "color",
      "id": "color_background",
      "label": "Background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_maroon",
      "label": "Heading",
      "default": "#7B1315"
    },
    {
      "type": "color",
      "id": "color_muted",
      "label": "City text",
      "default": "#5E5147"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Name text",
      "default": "#2A1C18"
    },
    {
      "type": "color",
      "id": "color_green",
      "label": "Verified tick",
      "default": "#1E7A4D"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "content_width",
      "label": "Content width",
      "min": 400,
      "max": 900,
      "step": 20,
      "unit": "px",
      "default": 480
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 30
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 30
    }
  ],
  "blocks": [
    {
      "type": "clip",
      "name": "Video",
      "settings": [
        {
          "type": "video",
          "id": "video",
          "label": "Video (uploaded to Shopify)",
          "info": "Best option — served from Shopify's CDN and loads nothing until tapped."
        },
        {
          "type": "video_url",
          "id": "video_url",
          "label": "Or a YouTube / Vimeo link",
          "accept": [
            "youtube",
            "vimeo"
          ]
        },
        {
          "type": "image_picker",
          "id": "poster",
          "label": "Cover image",
          "info": "Shown before the video plays. Portrait (9:16) works best."
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "info": "The actual person in the clip.",
          "default": "Anita R."
        },
        {
          "type": "text",
          "id": "city",
          "label": "City",
          "default": "Nagpur"
        },
        {
          "type": "text",
          "id": "placeholder_note",
          "label": "Placeholder note",
          "info": "Shown in the empty tile until you add a video or cover image.",
          "default": "UGC video"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "EIS — Video testimonials",
      "blocks": [
        {
          "type": "clip",
          "settings": {
            "name": "Anita R.",
            "city": "Nagpur",
            "placeholder_note": "UGC video — knee pain"
          }
        },
        {
          "type": "clip",
          "settings": {
            "name": "Vikram S.",
            "city": "Jaipur",
            "placeholder_note": "UGC video — back pain"
          }
        },
        {
          "type": "clip",
          "settings": {
            "name": "Dr. Meera N.",
            "city": "Kochi",
            "placeholder_note": "Vaidya / expert clip"
          }
        }
      ]
    }
  ]
}
{% endschema %}
